<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary" type="color">@color/primary</item>
        <item name="colorPrimaryDark" type="color">@color/primaryDark</item>
        <item name="colorAccent" type="color">@color/accent</item>

    </style>

    <style name="AppTheme.ActionBar"
           parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="android:height">56dp</item>
        <item name="android:width">110dp</item>
        <!-- Not working. Temporarily set to WHITE in the fragment(s)  -->
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.Title</item>
        <item name="android:background">@color/primary</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="AppTheme.ActionBar.Title" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">#000000</item>
    </style>


</resources>
